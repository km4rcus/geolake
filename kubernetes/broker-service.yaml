apiVersion: v1
kind: Service
metadata:
  namespace: geodds
  labels:
    geodds.service: broker
  name: broker
spec:
  ports:
    - name: "broker"
      port: 5672
      targetPort: 5672
  selector:
    geodds.service: broker
