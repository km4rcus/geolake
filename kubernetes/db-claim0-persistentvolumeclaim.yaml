apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: geodds
  labels:
    geodds.service: db-claim1
  name: db-claim1
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
